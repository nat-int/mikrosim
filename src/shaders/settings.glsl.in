struct particle {
	vec2 pos;
	uint type;
	float density;
	vec2 vel;
};
struct particle_id {
	vec2 pos;
	uint type;
	float density;
	vec2 vel;
	uint id;
	vec4 concentration;
};
const float mix_factors[] = float[4](0,1,1,0);
const uint CELLS_X = ${CELLS_X};
const uint CELLS_Y = ${CELLS_Y};
